import { backgroundColors, borderColors } from '../../../types/colors';

export default interface TextboxType {
  children?: React.ReactNode;
  backgroundColor: backgroundColors;
  borderColor: borderColors;
}
